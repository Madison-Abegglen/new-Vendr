export default class Target {
    constructor() {
        this.health = data.health
        this.name = data.name
        this.attacks = data.attacks
        this.items = data.items
    }

    attack(type) {
    }
}